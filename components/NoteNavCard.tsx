import React from 'react';
import { Note } from '../store/frontend.types';

interface NoteNavCardProps {
	note: Note;
}

const NoteNavCard: React.FC<NoteNavCardProps> = ({}) => {
	return <></>;
};

export default NoteNavCard;
